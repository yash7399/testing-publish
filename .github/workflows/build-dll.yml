name: Build DLLs for SMSInterface

on:
  push:
    branches:
      - main
      - master
      - dll-artifacts
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: 🔹 Checkout repository
        uses: actions/checkout@v4

      - name: 🔹 Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: 🔹 Setup NuGet
        uses: NuGet/setup-nuget@v2

      - name: 🔹 Restore any NuGet packages
        run: nuget restore CommonExtranet.sln

      - name: 🔹 Build Website Project (precompile DLLs)
        shell: pwsh
        run: |
          Write-Host "🏗️ Starting ASP.NET precompilation..."
          $publishProj = "SMSInterface\website.publishproj"
          $outputDir = "dll"
          if (Test-Path $outputDir) { Remove-Item -Recurse -Force $outputDir }
          & "C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" `
            $publishProj `
            /p:Configuration=Release `
            /p:WebPublishMethod=FileSystem `
            /p:DeleteExistingFiles=True `
            /p:publishUrl=$outputDir `
            /p:OutputPath=$outputDir
          Write-Host "✅ Build completed successfully. DLLs are in the 'dll' folder."

      - name: 🔹 Upload DLLs as artifact
        uses: actions/upload-artifact@v4
        with:
          name: website-dlls
          path: dll/